


<%= attachment_image_tag @post, :image, :fill, 300, 300, fallback: "no_image.jpg",size:"100×100" %>
<p>住所</p>
<p><%= @post.address %></p>


<div id="favorites_buttons_<%= @post.id %>">
  <%= render partial: 'users/favorites/favorite', locals: { post: @post} %>
</div>
<!-- locals:{}の中身は@postをpostに入れてpostを_favorite.htmlで使えるようにするため。
partialとlocalsを消しても非同期は使える。その時は_favorite.htmlのpostを@postに変えて、create.jsのpartialとlocalsも消したらうまくいく。
<%= @post.id %>の部分はネストされてるからpost.idも必要ってこと？ -->


  <strong>投稿者
    <%= link_to users_user_path(@post.user) do %>
      <%= @post.user.name %>
    <% end %>
  </strong>



<div id="follow_buttons_<%= @post.id %>">
  <%= render partial: 'users/relationships/relationship', locals: { post: @post} %>
</div>
<!-- ここの<%= @post.id %>はpostのshowページだからpost_idが必ず必要になってくる。だから必要。
userのshowとかで書くなら<%= @user.id %>とかが必要だということ！ -->





<div id="map"></div>

<style>
#map{
  height: 150px;
  width:270px;
}
</style>
<script type="text/javascript">
    function initMap() {

        var test ={lat: <%= @post.latitude %>, lng: <%= @post.longitude %>};
        var map = new google.maps.Map(document.getElementById('map'), {
            zoom: 15,
            center: test
        });
        var transitLayer = new google.maps.TransitLayer();
        transitLayer.setMap(map);

        var contentString = '住所：<%= @post.address %>';
        var infowindow = new google.maps.InfoWindow({
            content: contentString
        });

        var marker = new google.maps.Marker({
            position:test,
            map: map,
            title: contentString
        });

        marker.addListener('click', function() {
            infowindow.open(map, marker);
        });
    }
</script>

<script async defer
        src="https://maps.googleapis.com/maps/api/js?v=3.exp&key=<%= ENV['GOOGLE_MAP_API_KEY'] %>&callback=initMap">
</script>

<div>
 <%= render partial: 'users/post_comments/form', locals: { post_comment: @post_comment, post: @post } %>
</div>



<div id="comments_area">
  <%= render partial: 'users/post_comments/index', locals: { post_comments: @post_comments } %>
</div>






